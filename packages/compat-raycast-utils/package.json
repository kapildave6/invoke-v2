{
  "name": "@raycast/utils",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Clean-room compatibility shim: re-exports Invoke's own hooks under the @raycast/utils import path so existing extensions port mechanically. Ships NO Raycast code (PLAN.md §5.0).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@invoke/utils": "*"
  }
}
