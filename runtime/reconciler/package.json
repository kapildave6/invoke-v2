{
  "name": "@invoke/reconciler",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Custom React reconciler (mutation mode) that turns a React tree into a render-mutation stream instead of DOM. PLAN.md §4.5 — the linchpin.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "dependencies": {
    "@invoke/schema": "*",
    "react-reconciler": "^0.31.0"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  }
}
