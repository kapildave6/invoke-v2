{
  "name": "@invoke/schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared wire contract between the native host and the extension runtime (render mutations + framed JSON-RPC).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" }
}
